name: practica2
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  record: ^4.3.1
  flutterfire_ui:
  equatable:
  flutter_bloc:
  cloud_firestore:
  flutter_shimmer:
  google_sign_in:
  firebase_auth:
  firebase_core:
  firebase_storage:
  http:
  url_launcher:
  avatar_glow: ^2.0.2
flutter_icons:
  android: "ic_launcher"
  ios: true
  image_path: "assets/images/icon.png"
flutter:
  assets:
    - assets/images/
    - assets/gifs/
  uses-material-design: true
